server.port=8080
# enable deterministic fallback while you set up Groq key (optional)
explanation.fallback.local=false

# set your groq key via env var mapping or directly (do NOT commit secrets)
# groq.api.key has been removed for security. Use env var: groq.api.key=${GROQ_API_KEY}
# optional fallback if you still have openai.api.key defined:
# openai.api.key has been removed for security. Use env var: openai.api.key=${OPENAI_API_KEY}